{
  "name": "@toolrouter/mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --env-file-if-exists=../../.env --import tsx src/server.ts",
    "start": "node --env-file-if-exists=../../.env --import tsx src/server.ts",
    "check": "tsc -p ../../tsconfig.json --noEmit"
  },
  "dependencies": {
    "@toolrouter/router-core": "file:../../packages/router-core",
    "tsx": "^4.21.0"
  }
}
